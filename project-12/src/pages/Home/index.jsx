import React from 'react';
import { connect } from 'react-redux';
import history from '../../util/history';

function Home() {
    return (
        <section id="home">

        </section>
    );
}
export default (Home);